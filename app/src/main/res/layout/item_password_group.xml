<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools"
    android:id="@+id/item_password_group_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/shape_rect"
    android:backgroundTint="@color/color_selector_content"
    android:paddingHorizontal="@dimen/dp15"
    android:paddingVertical="@dimen/dp10">

    <ImageView
        android:id="@+id/item_password_group_icon"
        android:layout_width="@dimen/dp30"
        android:layout_height="@dimen/dp30"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/cd_image"
        android:padding="@dimen/dp3"
        android:src="@drawable/ic_light_folder" />

    <TextView
        android:id="@+id/item_password_group_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/dp5"
        android:layout_toEndOf="@id/item_password_group_icon"
        android:contentDescription="@string/cd_image"
        android:textColor="@color/color_accent"
        android:textSize="@dimen/sp14"
        tool:text="默认分组" />
</RelativeLayout>